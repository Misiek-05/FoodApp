<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ShoppingList.ListActivity"
            android:label="Lista Zakupów">

        </activity>

        <activity
            android:name=".ShoppingList.AddActivity"
            android:label="Dodaj Produkt">

        </activity>

        <activity
            android:name=".Recipes.RecipesActivity"
            android:label="Przepisy">

        </activity>

        <activity
            android:name=".Recipes.AddRecipeActivity"
            android:label="Dodaj Przepis">

        </activity>

        <activity
            android:name=".Recipes.RecipeViewActivity"
            android:label="Przepis">

        </activity>

        <activity
            android:name=".Recipes.AddIngredientActivity"
            android:label="Przepis">

        </activity>

        <activity
            android:name=".Nutrition.NutritionActivity"
            android:label="Nutrition">

        </activity>

        <activity
            android:name=".Nutrition.NutritionCalculatorActivity"
            android:label="Nutrition">

        </activity>

        <activity
            android:name=".Nutrition.NutritionSingleIngredientActivity"
            android:label="Nutrition (per 100g)">

        </activity>

        <activity
            android:name=".Nutrition.AddToCalculatorActivity"
            android:label="Nutrition Calculator">

        </activity>
    </application>

</manifest>