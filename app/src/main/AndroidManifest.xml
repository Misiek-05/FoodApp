<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="Menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ShoppingList.ListActivity"
            android:label="Shopping List">

        </activity>

        <activity
            android:name=".ShoppingList.AddActivity"
            android:label="Add product">

        </activity>

        <activity
            android:name=".Recipes.RecipesActivity"
            android:label="Recipes">

        </activity>

        <activity
            android:name=".Recipes.AddRecipeActivity"
            android:label="Add Recipe">

        </activity>

        <activity
            android:name=".Recipes.RecipeViewActivity"
            android:label="Recipe">

        </activity>

        <activity
            android:name=".Recipes.AddIngredientActivity"
            android:label="Add ingredient">

        </activity>

        <activity
            android:name=".Nutrition.NutritionActivity"
            android:label="Nutritional values">

        </activity>

        <activity
            android:name=".Nutrition.NutritionCalculatorActivity"
            android:label="Nutritional values calculator">

        </activity>

        <activity
            android:name=".Nutrition.NutritionSingleIngredientActivity"
            android:label="Nutritional values (per 100g)">

        </activity>

        <activity
            android:name=".Nutrition.AddToCalculatorActivity"
            android:label="Add product to calculator">

        </activity>
    </application>

</manifest>